@model EducationViewModel

@{
    ViewData["Title"] = "Education";
}
<h2 class="h3 fw-semibold mb-4" data-aos="fade-down">Education</h2>
<div class="vstack gap-4">
    @foreach (var education in Model.Educations)
    {
        <div class="glass-card p-4" data-aos="fade-up">
            <h3 class="h5">@education.CourseOfStudy</h3>
            <p class="text-muted">@education.Institution — @education.Duration</p>   
            <p class="text-muted">Percentage: @education.Percentage</p>   
        </div>
    }
</div>
